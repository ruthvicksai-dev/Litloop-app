export const BOOKS_PAGINATION_OPTS = {
    cursor: null,
    numItems: 20,
};

export const SERIES_PAGINATION_OPTS = {
    cursor: null,
    numItems: 20,
};

export const RENTALS_PAGINATION_OPTS = {
    cursor: null,
    numItems: 50,
};
